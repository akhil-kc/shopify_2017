<!DOCTYPE html>
<html>
<head>
<style>
table {
    font-family: arial, sans-serif;
    border-collapse: collapse;
    width: 100%;
}

td, th {
    border: 1px solid #dddddd;
    text-align: left;
    padding: 8px;
}

tr:nth-child(even) {
    background-color: #dddddd;
}
</style>
</head>
<body>

<center><h1>Orders Summary</h1></center>
 
<%= link_to 'New order/Upload from Shopify URL', new_order_path %>

<h2>Total Price: <%= @totalprice%> </h2>
<h2>Total Tax: <%= @totaltax %> </h2>

<table>
  <tr>
    <th>Order Id</th>
    <th>Item Name</th>
    <th>Total Price</th>
    <th>Total Tax</th>
  </tr>
 
  <% @orders.each do |order| %>
    <tr>
      <td><%= order.orderid%></td>
      <td><%= order.item_name%></td>
      <td><%= order.total_price%></td>
      <td><%= order.total_tax%></td>
      <td><%= link_to 'Show', order_path(order) %></td>
      <td><%= link_to 'Destroy', order_path(order),
              method: :delete,
              data: { confirm: 'Are you sure you want to delete this entry?' } %></td>
    </tr>
  <% end %>
</table>
</body>
</html>
